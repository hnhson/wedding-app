import type { CardConfig } from '@/types/card'
export default function ContentPanel({ config, onChange }: { config: CardConfig; onChange: (p: Partial<CardConfig>) => void }) {
  return <div>Content Panel (stub)</div>
}

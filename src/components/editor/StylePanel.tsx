import type { CardConfig } from '@/types/card'
export default function StylePanel({ config, onChange }: { config: CardConfig; onChange: (p: Partial<CardConfig>) => void }) {
  return <div>Style Panel (stub)</div>
}

import type { CardConfig } from '@/types/card'
export default function MapPanel({ config, onChange }: { config: CardConfig; onChange: (p: Partial<CardConfig>) => void }) {
  return <div>Map Panel (stub)</div>
}

import type { CardConfig } from '@/types/card'
export default function MediaPanel({ config, onChange, cardId }: { config: CardConfig; onChange: (p: Partial<CardConfig>) => void; cardId: string }) {
  return <div>Media Panel (stub)</div>
}
